import { IStore } from '../app/types';
/**
 * Displays the notification for linking the meeting to Salesforce.
 *
 * @returns {void}
 */
export declare function showSalesforceNotification(): (dispatch: IStore['dispatch'], getState: IStore['getState']) => void;
