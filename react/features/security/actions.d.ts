import { IStore } from '../app/types';
/**
 * Action that triggers toggle of the security options dialog.
 *
 * @returns {Function}
 */
export declare function toggleSecurityDialog(): (dispatch: IStore['dispatch']) => void;
