/**
 * Timeout of the conference when iframe is disabled in minutes.
 */
export declare const IFRAME_DISABLED_TIMEOUT_MINUTES = 5;
/**
 * A list of allowed location to embed iframe.
 */
export declare const IFRAME_EMBED_ALLOWED_LOCATIONS: string[];
