/**
 * Creates a React Native Paper theme based on local UI tokens.
 *
 * @param {Object} arg - The ui tokens.
 * @returns {Object}
 */
export declare function createNativeTheme({ font, colors, colorMap, shape, spacing, typography }: any): any;
