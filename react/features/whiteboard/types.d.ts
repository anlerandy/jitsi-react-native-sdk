/**
 * Whiteboard statuses used to raise the notification when it's changed.
 *
 * @enum
 */
export declare enum WhiteboardStatus {
    FORBIDDEN = "FORBIDDEN",
    HIDDEN = "HIDDEN",
    INSTANTIATED = "INSTANTIATED",
    RESET = "RESET",
    SHOWN = "SHOWN"
}
